#!/usr/bin/env python

import sys, os

sys.path.append('os.path.dirname(__file__)')

from app import app
from app.utils.log_helper import general

app.run(host=general['host'], port=int(general['port']), debug=general['debug'])
